baseURL = 'https://note.ironnori.cc/'
languageCode = 'en-us'
title = 'English Learning Note'
theme = 'scmp-learner'
hasCJKLanguage = true
summaryLength = 60

[outputs]
 home = ["HTML", "RSS", "JSON"]

[params]
 mainSections = ["posts", "bbc-english"]
 description = "One lesson at a time — scroll, tap in, and immerse yourself in real English, annotated for learners."
 author = "English Learning Note"
